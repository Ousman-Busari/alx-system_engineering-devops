#!/usr/bin/env bash
# Configuring nginx to listen on port 80 - the default port
ln -sf /etc/nginx/sites-available/defualt /etc/nginx/sitets-enabled/
service nginx restart
kill "$(pgrep 'nginx' | head -1)"
